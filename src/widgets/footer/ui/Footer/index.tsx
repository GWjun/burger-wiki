import * as styles from './styles.css';

export function Footer() {
  return (
    <footer className={styles.footer}>
      <p className={styles.comment}>{'"지식은 햄버거를 대신할 수 없어"'}</p>
    </footer>
  );
}
